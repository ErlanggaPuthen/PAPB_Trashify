import 'package:flutter/material.dart';

class HasilRiwayatPrediksi extends StatelessWidget {
  const HasilRiwayatPrediksi({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ini adalah Riwayat Prediksi'),
    );
  }
}
